<dependencies>

    <!-- Spring Starters -->
    ...

    <!-- Database -->
    ...

    <!-- Security -->
    ...

    <!-- Utilities -->
    ...

    <!-- Dev tools -->
    ...

    <!-- Testing -->
    ...

</dependencies>


we use LOMBOK to generate the getter and setter and constructor also
the constructor uses the final marked fields to generate the constructor


we have commented out all the code related to H2 db as we are using DOCKER











this is the earlier configuration of application.propertires
#spring.application.name=jobportal
#logging.pattern.console==${CONSOLE_LOG_PATTERN:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %cyan(%msg%n)}

#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.url=jdbc:h2:file:C:/ayush1/SpringBoot New Series/Spring Applications;AUTO_SERVER=true


#spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:jobportal}
#spring.datasource.username=${DATABASE_USERNAME:root}
#spring.datasource.password=${DATABASE_PASSWORD:root}

spring.application.name=jobportal
logging.pattern.console=${CONSOLE_LOG_PATTERN:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %cyan(%msg%n)}

spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:jobportal}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:root}
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}

spring.docker.compose.lifecycle-management=start-and-stop
spring.docker.compose.start.command=up
spring.docker.compose.stop.command=stop





#spring.datasource.username=sa
#spring.datasource.password=

#spring.sql.init.mode=always
#spring.datasource.driver-class-name=org.h2.Driver

#spring.jpa.show-sql=${SHOW_SQL:true}
#spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}
#spring.sql.init.mode=always
#spring.jpa.defer-datasource-initialization=true
#spring.docker.compose.lifecycle-management=start-and-stop
#spring.docker.compose.start.command=up
#spring.docker.compose.stop.command=stop














